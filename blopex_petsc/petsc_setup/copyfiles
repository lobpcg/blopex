# copy interface files to petsc-dev
from=$HOME/work/trunk/blopex_petsc/
to=$HOME/petsc-base/src/contrib/blopex/
for file in `cat interface_list`
do
  echo $file
  cp $from$file $to$file
done
for file in `cat test_list`
do
  echo $file
  cp $from$file $to$file
done
